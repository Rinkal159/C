* 3 byte = white
meaning
rgb(255, 255, 255) = white, because every section of rgb holds one byte(8 bits).
because

128  64  32  16  8  4  2  1 
 1    1   1   1  1  1  1  1

→ it sums up to 255, and it is 8 bits meaning 1 byte, that is why every rgb section is equal to one byte or less..

→ one F in hexadecimal equals to 15 - means 1111 in binary (10 in hexadimal equals to 16, and 0F is 15)
→ thus two F(FF) equlas to 1111 1111 thats sums ups to 255.

→ Hexadicaml has a prefix pattern that indicates that any number follows this pattern is a hexadecimal number.
  → that prefix is '0x'
    meaning, 
      decimal 16:
        in binary - 0b10000
        in hexadecimal - 0x10

  
  * 3 == 7
  → 3 means our three fingers.
  → 7 means our three fingers too! 111 in binary.